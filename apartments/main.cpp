#include <bits/stdc++.h>

#define FOR(x) for(int i=0;i<x;i++)
using namespace std;

int main(){
    int n,m,k;
    cin >> n >> m >> k;
    int applicanDesire[n];
    int apartmentSize[n];
    FOR(n){cin >> applicanDesire[i];}
    FOR(m){cin >> apartmentSize[i];}
    int count = 0;
    FOR(n){
        if()
        
    }

    return 0;
}